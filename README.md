# Read Minutes

Read Minutes is a Chrome extension that estimates how long a blog post or newsletter will take to read.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run test`

## Load In Chrome

1. Run `npm install`.
2. Run `npm run build`.
3. Open `chrome://extensions`.
4. Turn on **Developer mode**.
5. Choose **Load unpacked**.
6. Select the `dist` folder from this project.

## Current Status

The scaffold is in place. The next commits add the page analysis, inline badge, popup details, and settings.

